import React from 'react';
import ReactDOM from 'react-dom/client';
import VibeGallery from './components/InputDesign';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
    <React.StrictMode>
        <VibeGallery />
    </React.StrictMode>
); 
