# TypeScript + React 프로젝트 시작 체크리스트

## 1. 프로젝트 설정
- [x] Node.js 설치 확인 (`node -v`, `npm -v`)
- [x] 프로젝트 초기화
```bash
# 수동 설치 방식
mkdir my-react-app
cd my-react-app
npm init -y
```

## 2. 기본 패키지 설치
- [x] React 설치
```bash
npm install react
```
- [x] React DOM 설치
```bash
npm install react-dom
```
- [x] TypeScript 설치
```bash
npm install --save-dev typescript
```
- [x] React 타입 정의 설치
```bash
npm install --save-dev @types/react @types/react-dom
```

## 3. 기본 설정
- [x] `package.json` 스크립트 설정
```json
{
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html"
  }
}
```
- [x] `tsconfig.json` 설정
```bash
npx tsc --init
```
- [ ] ESLint 설정
- [x] `.gitignore` 파일 확인
```text
# Dependencies
node_modules/
.pnp/
.pnp.js

# Testing
coverage/

# Production
dist/
build/

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
```

## 4. 프로젝트 구조 설정
- [ ] 기본 디렉토리 구조 생성
- [ ] 기본 컴포넌트 생성
- [ ] 라우팅 설정

## 5. 스타일링
- [ ] CSS 모듈 또는 styled-components 설치
```bash
# CSS Modules (기본 포함)
# 또는 styled-components
npm install styled-components @types/styled-components
```
- [ ] 기본 스타일링 설정
- [ ] 전역 스타일 설정 (`src/styles/global.css`)

## 6. 상태 관리
- [ ] Context API 사용 연습
- [ ] Redux 또는 Recoil 설치
```bash
# Redux
npm install @reduxjs/toolkit react-redux

# 또는 Recoil
npm install recoil
```

## 7. 라우팅
- [ ] React Router 설치
```bash
npm install react-router-dom @types/react-router-dom
```

## 8. API 통신
- [ ] Axios 설치
```bash
npm install axios
```
- [ ] API 호출 함수 작성
```typescript
// src/api/api.ts
import axios from 'axios';

const api = axios.create({
  baseURL: 'https://api.example.com',
  timeout: 1000,
});

export const fetchData = async () => {
  try {
    const response = await api.get('/data');
    return response.data;
  } catch (error) {
    console.error('API Error:', error);
    throw error;
  }
};
```

## 9. 테스트
- [ ] Jest 설정 확인
- [ ] React Testing Library 설치
```bash
npm install --save-dev @testing-library/react @testing-library/jest-dom
```
## 10. 빌드 및 배포
- [ ] `npm run build` 테스트
- [ ] 배포 환경 설정
```bash
# Netlify CLI
npm install -g netlify-cli

# Vercel CLI
npm install -g vercel
```

## 11. 추가 기능 (선택사항)
- [ ] PWA 설정
- [ ] SEO 최적화
- [ ] 성능 최적화

